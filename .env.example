# Mobile production API URL is set in code:
#   src/config/appConfig.ts  →  PRODUCTION_API_BASE_URL (HTTPS, must include /api if your server uses it)
#
# Backend (Azure / Docker) secrets belong in `.env.prod` (see `.env.prod.example`), not in this file.
