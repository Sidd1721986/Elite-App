# Copy to keystore.properties for local release builds (file is gitignored).
# In CI, the pipeline generates this file from a secure keystore + secret variables.
#
# storeFile=/absolute/path/to/your-release.keystore
# storePassword=...
# keyAlias=...
# keyPassword=...
