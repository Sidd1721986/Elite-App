# Copy to keystore.properties (same directory) and fill in real values.
# keystore.properties is gitignored — never commit secrets.
#
# storeFile is relative to android/app/ (e.g. ../my-release-key.jks)
#
# storePassword=...
# keyPassword=...
# keyAlias=...
# storeFile=../your-upload-key.jks
