/**
 * moti/skeleton (build) imports `expo-linear-gradient`. This app is bare React Native
 * and already links `react-native-linear-gradient`; map the Expo named export here.
 */
import RNLinearGradient from 'react-native-linear-gradient';

export const LinearGradient = RNLinearGradient;
