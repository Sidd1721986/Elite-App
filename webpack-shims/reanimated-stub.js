/**
 * Optional peer of react-native-gesture-handler; webpack resolves it statically.
 * Stub avoids "module not found" and keeps the gesture-handler code path that expects no Reanimated APIs.
 */
module.exports = {};
